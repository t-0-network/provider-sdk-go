// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tzero/v1/payment_intent/beneficiary.proto

package payment_intentconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	payment_intent "github.com/t-0-network/provider-sdk-go/api/tzero/v1/payment_intent"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// BeneficiaryServiceName is the fully-qualified name of the BeneficiaryService service.
	BeneficiaryServiceName = "tzero.v1.payment_intent.BeneficiaryService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// BeneficiaryServicePaymentIntentUpdateProcedure is the fully-qualified name of the
	// BeneficiaryService's PaymentIntentUpdate RPC.
	BeneficiaryServicePaymentIntentUpdateProcedure = "/tzero.v1.payment_intent.BeneficiaryService/PaymentIntentUpdate"
)

// BeneficiaryServiceClient is a client for the tzero.v1.payment_intent.BeneficiaryService service.
type BeneficiaryServiceClient interface {
	// *
	// PaymentIntentUpdate notifies the beneficiary provider of status changes.
	//
	// Currently supports:
	// - FundsReceived: The pay-in provider confirmed receipt
	//
	// This notification is sent after ledger entries are created and the balance
	// has been updated. The beneficiary provider can use this to:
	// - Update their internal records
	// - Notify their end-user of successful payment
	// - Trigger downstream processing (e.g., release goods/services)
	//
	// Idempotency: This endpoint must be idempotent. The network may retry
	// delivery in case of failures or timeouts.
	PaymentIntentUpdate(context.Context, *connect.Request[payment_intent.PaymentIntentUpdateRequest]) (*connect.Response[payment_intent.PaymentIntentUpdateResponse], error)
}

// NewBeneficiaryServiceClient constructs a client for the
// tzero.v1.payment_intent.BeneficiaryService service. By default, it uses the Connect protocol with
// the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To use
// the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewBeneficiaryServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) BeneficiaryServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	beneficiaryServiceMethods := payment_intent.File_tzero_v1_payment_intent_beneficiary_proto.Services().ByName("BeneficiaryService").Methods()
	return &beneficiaryServiceClient{
		paymentIntentUpdate: connect.NewClient[payment_intent.PaymentIntentUpdateRequest, payment_intent.PaymentIntentUpdateResponse](
			httpClient,
			baseURL+BeneficiaryServicePaymentIntentUpdateProcedure,
			connect.WithSchema(beneficiaryServiceMethods.ByName("PaymentIntentUpdate")),
			connect.WithIdempotency(connect.IdempotencyIdempotent),
			connect.WithClientOptions(opts...),
		),
	}
}

// beneficiaryServiceClient implements BeneficiaryServiceClient.
type beneficiaryServiceClient struct {
	paymentIntentUpdate *connect.Client[payment_intent.PaymentIntentUpdateRequest, payment_intent.PaymentIntentUpdateResponse]
}

// PaymentIntentUpdate calls tzero.v1.payment_intent.BeneficiaryService.PaymentIntentUpdate.
func (c *beneficiaryServiceClient) PaymentIntentUpdate(ctx context.Context, req *connect.Request[payment_intent.PaymentIntentUpdateRequest]) (*connect.Response[payment_intent.PaymentIntentUpdateResponse], error) {
	return c.paymentIntentUpdate.CallUnary(ctx, req)
}

// BeneficiaryServiceHandler is an implementation of the tzero.v1.payment_intent.BeneficiaryService
// service.
type BeneficiaryServiceHandler interface {
	// *
	// PaymentIntentUpdate notifies the beneficiary provider of status changes.
	//
	// Currently supports:
	// - FundsReceived: The pay-in provider confirmed receipt
	//
	// This notification is sent after ledger entries are created and the balance
	// has been updated. The beneficiary provider can use this to:
	// - Update their internal records
	// - Notify their end-user of successful payment
	// - Trigger downstream processing (e.g., release goods/services)
	//
	// Idempotency: This endpoint must be idempotent. The network may retry
	// delivery in case of failures or timeouts.
	PaymentIntentUpdate(context.Context, *connect.Request[payment_intent.PaymentIntentUpdateRequest]) (*connect.Response[payment_intent.PaymentIntentUpdateResponse], error)
}

// NewBeneficiaryServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewBeneficiaryServiceHandler(svc BeneficiaryServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	beneficiaryServiceMethods := payment_intent.File_tzero_v1_payment_intent_beneficiary_proto.Services().ByName("BeneficiaryService").Methods()
	beneficiaryServicePaymentIntentUpdateHandler := connect.NewUnaryHandler(
		BeneficiaryServicePaymentIntentUpdateProcedure,
		svc.PaymentIntentUpdate,
		connect.WithSchema(beneficiaryServiceMethods.ByName("PaymentIntentUpdate")),
		connect.WithIdempotency(connect.IdempotencyIdempotent),
		connect.WithHandlerOptions(opts...),
	)
	return "/tzero.v1.payment_intent.BeneficiaryService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case BeneficiaryServicePaymentIntentUpdateProcedure:
			beneficiaryServicePaymentIntentUpdateHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedBeneficiaryServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedBeneficiaryServiceHandler struct{}

func (UnimplementedBeneficiaryServiceHandler) PaymentIntentUpdate(context.Context, *connect.Request[payment_intent.PaymentIntentUpdateRequest]) (*connect.Response[payment_intent.PaymentIntentUpdateResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tzero.v1.payment_intent.BeneficiaryService.PaymentIntentUpdate is not implemented"))
}
