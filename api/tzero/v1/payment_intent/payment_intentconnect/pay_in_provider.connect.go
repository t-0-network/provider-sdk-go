// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: tzero/v1/payment_intent/pay_in_provider.proto

package payment_intentconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	payment_intent "github.com/t-0-network/provider-sdk-go/api/tzero/v1/payment_intent"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PayInProviderServiceName is the fully-qualified name of the PayInProviderService service.
	PayInProviderServiceName = "tzero.v1.payment_intent.PayInProviderService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PayInProviderServiceGetPaymentDetailsProcedure is the fully-qualified name of the
	// PayInProviderService's GetPaymentDetails RPC.
	PayInProviderServiceGetPaymentDetailsProcedure = "/tzero.v1.payment_intent.PayInProviderService/GetPaymentDetails"
)

// PayInProviderServiceClient is a client for the tzero.v1.payment_intent.PayInProviderService
// service.
type PayInProviderServiceClient interface {
	// *
	// GetPaymentDetails returns payment details for the end-user.
	//
	// Called by the network during CreatePaymentIntent processing.
	// The provider should return payment details (bank accounts, mobile money info, etc.)
	// that the end-user can use to send funds.
	//
	// The provider may return details for multiple payment methods if requested.
	// Each returned PaymentDetails will be presented to the end-user as a payment option.
	//
	// Important considerations:
	// - Payment details should be valid for the expected payment timeframe
	// - Include any reference numbers needed to identify incoming payments
	// - The provider is responsible for monitoring incoming payments and calling ConfirmFundsReceived
	GetPaymentDetails(context.Context, *connect.Request[payment_intent.GetPaymentDetailsRequest]) (*connect.Response[payment_intent.GetPaymentDetailsResponse], error)
}

// NewPayInProviderServiceClient constructs a client for the
// tzero.v1.payment_intent.PayInProviderService service. By default, it uses the Connect protocol
// with the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To
// use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb()
// options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPayInProviderServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PayInProviderServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	payInProviderServiceMethods := payment_intent.File_tzero_v1_payment_intent_pay_in_provider_proto.Services().ByName("PayInProviderService").Methods()
	return &payInProviderServiceClient{
		getPaymentDetails: connect.NewClient[payment_intent.GetPaymentDetailsRequest, payment_intent.GetPaymentDetailsResponse](
			httpClient,
			baseURL+PayInProviderServiceGetPaymentDetailsProcedure,
			connect.WithSchema(payInProviderServiceMethods.ByName("GetPaymentDetails")),
			connect.WithIdempotency(connect.IdempotencyIdempotent),
			connect.WithClientOptions(opts...),
		),
	}
}

// payInProviderServiceClient implements PayInProviderServiceClient.
type payInProviderServiceClient struct {
	getPaymentDetails *connect.Client[payment_intent.GetPaymentDetailsRequest, payment_intent.GetPaymentDetailsResponse]
}

// GetPaymentDetails calls tzero.v1.payment_intent.PayInProviderService.GetPaymentDetails.
func (c *payInProviderServiceClient) GetPaymentDetails(ctx context.Context, req *connect.Request[payment_intent.GetPaymentDetailsRequest]) (*connect.Response[payment_intent.GetPaymentDetailsResponse], error) {
	return c.getPaymentDetails.CallUnary(ctx, req)
}

// PayInProviderServiceHandler is an implementation of the
// tzero.v1.payment_intent.PayInProviderService service.
type PayInProviderServiceHandler interface {
	// *
	// GetPaymentDetails returns payment details for the end-user.
	//
	// Called by the network during CreatePaymentIntent processing.
	// The provider should return payment details (bank accounts, mobile money info, etc.)
	// that the end-user can use to send funds.
	//
	// The provider may return details for multiple payment methods if requested.
	// Each returned PaymentDetails will be presented to the end-user as a payment option.
	//
	// Important considerations:
	// - Payment details should be valid for the expected payment timeframe
	// - Include any reference numbers needed to identify incoming payments
	// - The provider is responsible for monitoring incoming payments and calling ConfirmFundsReceived
	GetPaymentDetails(context.Context, *connect.Request[payment_intent.GetPaymentDetailsRequest]) (*connect.Response[payment_intent.GetPaymentDetailsResponse], error)
}

// NewPayInProviderServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPayInProviderServiceHandler(svc PayInProviderServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	payInProviderServiceMethods := payment_intent.File_tzero_v1_payment_intent_pay_in_provider_proto.Services().ByName("PayInProviderService").Methods()
	payInProviderServiceGetPaymentDetailsHandler := connect.NewUnaryHandler(
		PayInProviderServiceGetPaymentDetailsProcedure,
		svc.GetPaymentDetails,
		connect.WithSchema(payInProviderServiceMethods.ByName("GetPaymentDetails")),
		connect.WithIdempotency(connect.IdempotencyIdempotent),
		connect.WithHandlerOptions(opts...),
	)
	return "/tzero.v1.payment_intent.PayInProviderService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PayInProviderServiceGetPaymentDetailsProcedure:
			payInProviderServiceGetPaymentDetailsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPayInProviderServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPayInProviderServiceHandler struct{}

func (UnimplementedPayInProviderServiceHandler) GetPaymentDetails(context.Context, *connect.Request[payment_intent.GetPaymentDetailsRequest]) (*connect.Response[payment_intent.GetPaymentDetailsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("tzero.v1.payment_intent.PayInProviderService.GetPaymentDetails is not implemented"))
}
